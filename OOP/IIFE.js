// the Immediately Invoked Function Expression (IIFE)








// an anonymouse fun expression that executes right away
// the function has no name and 
// is not stored in a variable
(() => {
    console.log('hi')
})()

// the () at the end of the fun
// cause it to be immediately executed